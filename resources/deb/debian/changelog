quadcast2srgb (0.4.0) unstable; urgency=medium

  * Added feature to capture audio. To enable usage requires `--capture-audio` or similar config property.
  * Added audio device selection via `--audio-device-id <n>` and config key `device-id`.
    Run `--list-audio-devices` to show available devices with their IDs.
  * Added channel selection via `--audio-channel <n>` and config key `channel`
    (0 = left, 1 = right). Requires `--capture-audio`.
  * Added `--input-gain <float>` / `input-gain` and `--audio-smoothing-alpha <float>` /
    `audio-smoothing-alpha` config keys for audio capture adjustments.
  * Added `--list-audio-devices` flag to list all available PortAudio devices and exit.
  * Added default audio-reactive shader `audiosphere.glsl` to the resources.
  * Added `u_audioVolume` uniform (also `iAudioVolume`, `audioVolume`) to GLSL shader
    display, reports current capture volume level (0–1).
  * Unknown CLI arguments now produce an error and suggest `--help`.
  * Displays now wait for the device to acknowledge LED packets by default.
    Added `--no-wait-for-read` / `no-wait-for-read` config key to skip this wait.

 -- Mario T <admin@muffinmar.io>  Fri, 05 Jun 2026 00:00:00 +0000

quadcast2srgb (0.3.0) unstable; urgency=medium

  * New display `glsl`: renders an OpenGL ES 3.00 fragment shader to the LED grid via EGL headless rendering.
    New CLI args: --shader-path (required), --shader-fps (default 30), --shader-scale (default 1, supersampling).
    New config keys for `glsl`: shader-path, shader-fps, shader-scale.
    Accepts Shadertoy-style uniform names (iTime, iResolution, iFrame) and u_* / bare aliases.

 -- Mario T <admin@muffinmar.io>  Fri, 30 May 2026 00:00:00 +0000

quadcast2srgb (0.2.1) unstable; urgency=medium

  * Bugfix: rainbow display hue wrap-around now correct for negative rotation speeds.
  * Default config updated: static solid color replaced with a subtle color transition.

 -- Mario T <admin@muffinmar.io>  Wed, 28 May 2026 00:00:00 +0000

quadcast2srgb (0.2.0) unstable; urgency=medium

  * New display `transition`: cycling through n>=2 colors. 
    New CLI args: --transition-colors (comma-separated colors), --transition-speed, --transition-cubic-bezier. 
    New config args for `transition`: transition-colors, transition-speed, bezier.
  * New display `rainbow`: static or rolling rainbow across all LEDs.
    New CLI args: --rainbow-mode (flat|vertical|horizontal|diagonal), --rainbow-speed.
    New config args for `rainbow`: rainbow-mode, rainbow-speed.

 -- Mario T <admin@muffinmar.io>  Wed, 28 May 2026 00:00:00 +0000

quadcast2srgb (0.1.0) unstable; urgency=medium

  * Initial Debian packaging.

 -- Mario T <admin@muffinmar.io>  Tue, 18 Mar 2026 00:00:00 +0000
